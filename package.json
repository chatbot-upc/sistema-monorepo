{
  "name": "chatbot-upc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev:web dev:api dev:worker",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "cd apps/api && uv run uvicorn chatbot_api.main:app --reload --port 8000",
    "dev:worker": "cd apps/api && uv run celery -A chatbot_api.core.celery_app worker --pool=solo --loglevel=info",
    "build:web": "pnpm --filter web build",
    "lint:web": "pnpm --filter web lint",
    "test:api": "cd apps/api && uv run pytest",
    "lint:api": "cd apps/api && uv run ruff check . && uv run mypy"
  },
  "packageManager": "pnpm@10.32.1"
}
